## Dependencies note

The `openai` package is installed manually in the server virtualenv
due to pipenv + Python 3.12 resolver issues.

Server setup requires:
pipenv shell
pip install openai==1.109.1
